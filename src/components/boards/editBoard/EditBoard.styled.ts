import styled from 'styled-components';
import { GlobalControls, SubmitButton } from '../../../GlobalStyles';

export const Controls = styled(GlobalControls)``;

export const ActionButton = styled(SubmitButton)``;
